import React from 'react';

const Header = () => (
  <section className="hero is-success is-fullheight hero-image">
    <div className="hero-body" />
    <a className="button">CONTACT</a>
  </section>
);

export default Header;
